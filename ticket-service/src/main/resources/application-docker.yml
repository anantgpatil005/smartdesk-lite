server:
  port: 8090

spring:
  application:
    name: ticket-service

  datasource:
    url: jdbc:postgresql://postgres:5432/ticket_db
    password: root
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true